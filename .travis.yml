language: rust
cache: cargo
install: '[ "`cobalt --version`" == "Cobalt 0.11.1" ] || cargo install --force cobalt-bin'
script:
  - cargo test
  - make
rust:
  - stable
  - beta
matrix:
  allow_failures:
    - rust: beta
